# Comma-separated accounts that may manage the catalog. Set this only in the server environment.
ADMIN_EMAILS=

# Keep this key only in the hosted runtime configuration, never in this file or the browser.
OPENAI_API_KEY=
# Optional: gpt-5.6-luna is the default for the StudentHub AI assistant.
OPENAI_MODEL=
